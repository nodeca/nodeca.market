- self.add_raw_data('search', self.search)
- self.add_raw_data('settings', self.settings)
- self.add_raw_data('pagination', self.pagination)

article#content.market-search-buy
  -
    const navbar_base = {
      level_up: self.link_to('market.index.buy'),
      progress: {
        link_top:    self.link_to('market.search.buy', { $query: self.search }),
        link_bottom: '',
        current:     0,
        total:       0
      }
    };

    const navbar_primary = {
      search_method: 'market.search.buy'
    };

    const navbar_alt = {
      title:    self.t('title'),
      extra_buttons: 'market.search.buy.blocks.page_actions.dropdown',
    };

  != self.partial('@common.blocks.navbar.levelup_progress_apps', { ...navbar_base, ...navbar_primary })
  != self.partial('@common.blocks.navbar.alt_levelup_progress_title', { ...navbar_base, ...navbar_alt })

  nav.breadcrumbs-container
    .layout__container.container-fluid
      != self.partial('@common.blocks.breadcrumbs')

  .layout__container.container-fluid
    != self.partial('@common.blocks.announces')
    != self.partial('@market.blocks.drafts')

    header.page-head.market-search-buy__head
      h1.market-search-buy__title.page-head__title(itemprop='name')= self.t('title')

      != self.partial('@market.search.buy.blocks.page_actions')

    != self.partial('@market.blocks.search_form_buy')

    .market-search-buy__results
      .market-search-buy__loading-next.nojs-hide
        span.icon.icon-loading-placeholder
